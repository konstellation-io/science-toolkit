apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: drone
  labels:
    app:  drone
spec:
  tls:
    - hosts:
        - drone.{{ .Values.domain }}
      secretName: {{ .Release.Name }}-tls-secret
  rules:
    - host: drone.{{ .Values.domain }}
      http:
        paths:
          - path: /
            backend:
              serviceName: drone
              servicePort: http
