stages:
  - semantic-release
  - publish
  - semantic-release-helm
  - publish-gitlab-release
  - build
  - deploy
include:
  - local: jupyterlab-gpu-image/.gitlab-ci.yml
  - local: autologin/.gitlab-ci.yml
  - local: helm/.gitlab-ci.yml
