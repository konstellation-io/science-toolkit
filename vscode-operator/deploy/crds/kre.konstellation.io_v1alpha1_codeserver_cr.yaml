apiVersion: sci-toolkit.konstellation.io/v1alpha1
kind: CodeServer
metadata:
  name: gustavo
spec:
  # Default values copied from <project_dir>/helm-charts/codeserver/values.yaml
  
  # Default values for codeserver.
  # This is a YAML-formatted file.
  # Declare variables to be passed into your templates.

  domain: toolkit.local
  auth:
    username: gustavo
    password: Aa-123456

  storage:
    size: 10Gi
    className: standard

  #sharedVolume:
  #  name: ""

  gitea:
    clientId: "123456"
    clientSecret: "123456"

  tls:
    enabled: false
